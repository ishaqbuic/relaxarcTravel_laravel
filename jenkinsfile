pipeline{
    agent any 
    stages{
        stage('Build'){
            steps{
                sh 'php composer.phar install'
                sh 'composer install'
                sh 'mv .env.example .env'
                sh 'apt install nodejs'
                sh 'npm install'
                sh 'php artisan key:generate'
            }
        }
    }
}

